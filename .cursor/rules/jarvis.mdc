---
description: Jarvis 个人交易参谋项目总规则（分层、MCP、HITL、能力层）
alwaysApply: true
---

# Jarvis 开发规则（摘要）

完整规则见 `rules/agent.md`，架构见 `docs/ARCHITECTURE.md`，视觉见 `docs/DESIGN.md`。知识库维护页仅管理员。

## 必须遵守

1. **分层**：api/mcp/agents → services → domain → infrastructure；业务只写在 `services/`。对话 RAG 在 `services/rag.py`。
2. **网页 vs MCP**：`bash start.sh` 启本机开发；服务器打包见 `deploy/`；MCP 是 stdio，由 Cursor **按需拉起**（`scripts/mcp.sh`）。
3. **写入**：站内对话用 patch + 用户确认；MCP 写入工具会直接改 **MySQL 业务表**，仅可信环境。
4. **存储**：业务 **MySQL**、知识向量 **Milvus** 均为产品路径（非可选项）；策略原文在 `knowledge/`。
5. **Python**：`backend/.venv` ≥ 3.10（推荐 3.12）；官方 `mcp` SDK。
6. **勿改** `bussness_harnessos` / 旧 dashboard 原仓；勿把企业平台多租户/服务器机密写进本仓。
7. 与用户沟通用**简体中文**。
